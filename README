From within the pywstemplate directory (where the bootstrap.py file is located) run:

> python bootstrap.py
> ./bin/buildout

To generate stub code:

> ./bin/gencode

Configure service (if needed) by editing pyserver.cfg. Note that the port in pyserver.cfg must correspond to the one defined in the WSDL.

Hello world service can be started either by:

> ./bin/python src/helloservice/helloservice.py

or:

> ./bin/serverd start

For serverd parameters:

> ./bin/serverd --help